package me.lovelace.loveclans.model.quest;

/** Weekly contracts run on a fixed calendar week (Mon-Sun); daily contracts run 24h from activation (§1.1). */
public enum ContractType {
    WEEKLY,
    DAILY
}
